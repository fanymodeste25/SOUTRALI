"""
URL configuration for webhooks app.
"""
from django.urls import path

app_name = 'webhooks'

urlpatterns = [
    # Webhook endpoints will be added here
    # path('wave/', WaveWebhookView.as_view(), name='wave'),
    # path('orange/', OrangeMoneyWebhookView.as_view(), name='orange'),
    # path('mtn/', MTNMoMoWebhookView.as_view(), name='mtn'),
]
